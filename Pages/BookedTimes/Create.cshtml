@page
@model Sauna.Pages.BookedTimes.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>BookedTime</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="BookedTime.ChoosedTime" class="control-label"></label>
                <input asp-for="BookedTime.ChoosedTime" class="form-control" />
                <span asp-validation-for="BookedTime.ChoosedTime" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="BookedTime.CustomerId" class="control-label"></label>
                <input asp-for="BookedTime.CustomerId" class="form-control" />
                <span asp-validation-for="BookedTime.CustomerId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
